{
  "name": "payments",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ verified-network/payments",
    "create-local": "graph create --node http://localhost:8020/ verified-network/payments",
    "remove-local": "graph remove --node http://localhost:8020/ verified-network/payments",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 verified-network/payments"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.23.2",
    "@graphprotocol/graph-ts": "0.23.1"
  }
}
